package org.processbase.ui.servlet;

import org.processbase.ui.core.template.PbWindow;

import com.vaadin.ui.FormLayout;
import com.vaadin.ui.Label;
import com.vaadin.ui.TextField;

public class MobileIdLoginWindow{
 /*extends Form {
	private FormLayout form;
	private TextField mobileNr;

	public MobileIdLoginWindow(String caption){
		super(caption);
		form = new FormLayout();
		mobileNr=new TextField("Mobiili nr");
		form.addComponent(mobileNr);
		form.getFooter().addComponent(
		        new Label("This is the footer area of the Form. "+
		                  "You can use any layout here. "+
		                  "This is nice for buttons."));
		confirmationCodeNr=new Label("")
	}*/
}
